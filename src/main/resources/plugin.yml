name: OneBlock
version: '1.1.2'
main: ch.waldnetworks.oneBlock.OneBlock
api-version: '1.21'
prefix: OneBlock
load: STARTUP
authors: [ The12Forest ]
description: "This Plugin provides a basic One-Block application. PS: It also works with Multiverse!"

libraries:
  - com.zaxxer:HikariCP:7.0.2

commands:
  oneblock:
    permission: "OneBlock.Admin"
    discription: The Command for the OneBlock plugin
    aliases:
      - ob
      - OneBlock
      - The12Forest
